#include<stdio.h>

int main()
{
int a,b,add,sub,div,mul;
	printf("Enter two numbers : ");
	scanf("%d %d",&a,&b);
		add=a+b;
		sub=a-b;
		div=a/b;
		mul=a*b;
	printf("addition = %d",add);
	printf("\nsubtraction = %d",sub);
	printf("\ndivision = %d",div);
	printf("\nmultiplication = %d",mul);
return 0;
}
new changes
